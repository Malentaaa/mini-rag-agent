services:
  mini-rag-agent:
    build: .
    container_name: mini-rag-agent
    ports:
      - "8000:8000"
    volumes:
      - ./data/docs:/app/data/docs
      - ./storage/indexes:/app/storage/indexes
      - ./logs:/app/logs
    restart: unless-stopped